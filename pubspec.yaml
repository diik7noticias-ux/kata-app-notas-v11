name: notas_v11
description: Notas V11 - Aplicação de gestão de notas com funcionalidade de pesquisa
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  intl: ^0.19.0
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  search_choices: ^2.3.1  # Versão compatível com Flutter 3.44.8

dev_dependencies:
  build_runner: ^2.4.6
  hive_generator: ^2.0.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true