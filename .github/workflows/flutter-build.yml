O ficheiro `.github/workflows/flutter-build.yml` não está relacionado com o pedido de melhoria de performance ou adição de funcionalidades ao app Flutter. Como o pedido refere-se à adição de uma aba de pesquisa no topo da interface do `HomeScreen`, vou manter o ficheiro atual intacto, uma vez que não é relevante para essa alteração.

Por favor, forneça o ficheiro `lib/screens/home_screen.dart` para que eu possa implementar a funcionalidade de pesquisa.